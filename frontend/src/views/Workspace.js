/**
 * 메인 워크스페이스 컴포넌트  
 * Todo App의 세부 내용을 표시하고 관리한다.
 * @return {HTMLElement} 메인 워크스페이스 컴포넌트
 */
const Workspace = () => {
    const el = document.createElement('section');
    el.className = 'workspace';

    return el;
};

export default Workspace;
